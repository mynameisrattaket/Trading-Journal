module.exports = {
    parser: '@babel/eslint-parser', // ใช้ Babel แทน parser ที่เป็นฟังก์ชัน
    extends: ['next', 'eslint:recommended'],
    rules: {
      // คุณสามารถตั้งค่ากฎเพิ่มเติมได้ที่นี่
    },
  };
  